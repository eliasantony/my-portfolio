<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
    <!-- Background circle (optional) -->
    <circle cx="50" cy="50" r="50" fill="#00C795" />
    <!-- Monogram -->
    <text
        x="50%"
        y="50%"
        text-anchor="middle"
        dominant-baseline="middle"
        font-family="Arial, sans-serif"
        font-weight="bold"
        font-size="60"
        fill="#ffffff"
        font-style="italic"
    >
        EA
    </text>
</svg>